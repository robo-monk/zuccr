<div class="form_centered">
<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.text_field :title, placeholder: "don't clickbait..", class: 'user_text_input' %>
  </div>
<br>
  <div class="field">
    <%= form.text_area :body, placeholder: "don't lorem ipsum this..", class: 'user_large_text_input'%>
  </div>
 <br>
  <div class="actions">
    <%= form.submit :post, class: 'submit_button' %>
  </div>
<% end %>
</div>